<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <title>新しい収支を追加</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <style>
    body {
      font-family: "Hiragino Sans", "Helvetica", sans-serif;
      margin: 0;
      padding-bottom: 120px; /* 下固定ボタンに余白を確保 */
      background: #fafafa;
    }

    h1 {
      font-size: 26px;
      margin: 20px;
      color: #333;
    }

    .container {
      padding: 0 20px 40px;
    }

    label {
      font-weight: bold;
      margin-top: 20px;
      display: block;
    }

    input, select, textarea {
      width: 100%;
      padding: 14px;
      font-size: 16px;
      border-radius: 10px;
      border: 1px solid #ddd;
      margin-top: 6px;
      background: #f3f3f3;
    }

    textarea {
      height: 140px;
    }

    /* ▼ 画面下に固定するボタン */
    .bottom-bar {
      position: fixed;
      bottom: 0;
      left: 0;
      width: 100%;
      background: #f7f4eb;
      padding: 20px;
      box-shadow: 0px -2px 10px rgba(0,0,0,0.08);
      display: flex;
      justify-content: center;
    }

    .submit-btn {
      width: 90%;
      padding: 15px;
      background: #d8cbb3;
      text-align: center;
      border-radius: 30px;
      color: #333;
      font-size: 18px;
      font-weight: bold;
      border: none;
    }

    .submit-btn:hover {
      background: #c9bea9;
    }

    /* 戻るリンク */
    .back {
      margin: 20px;
      display: inline-block;
      color: #333;
      text-decoration: none;
    }
  </style>
</head>

<body>

  <h1>新しい収支を追加</h1>

  <div class="container">
    <form action="/items/new" method="POST">

      <label>項目名</label>
      <input type="text" name="event" placeholder="例：ランチ代、給料など" required />

      <label>金額</label>
      <input type="number" name="amount" placeholder="例：1200" required />

      <label>区分</label>
      <select name="type" required>
        <option value="">選択してください</option>
        <option value="income">収入</option>
        <option value="expense">支出</option>
      </select>

      <label>メモ（任意）</label>
      <textarea name="memo" placeholder="必要であればメモを入力"></textarea>

      <!-- 画面下固定ボタン -->
      <div class="bottom-bar">
        <button type="submit" class="submit-btn">登録する</button>
      </div>

    </form>

    <a href="/items" class="back">← 一覧に戻る</a>
  </div>

</body>
</html>
